#!/bin/sh

if [ -z "${PROJECT_ROOT}" ]; then
    export PROJECT_ROOT="$(dirname "$(realpath $0)")/../.."
fi

if [ ! -z "$(docker ps -q)" ]; then
    "${PROJECT_ROOT}"/bin/docker/stopall
fi

docker volume rm dbvisor-caddy-config
docker volume rm dbvisor-caddy-data

"${PROJECT_ROOT}"/bin/docker/start -b