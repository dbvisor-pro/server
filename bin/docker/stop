#!/usr/bin/env bash

if [ -z "${PROJECT_ROOT}" ]; then
    export PROJECT_ROOT="$( cd "$(dirname "$0")/../../" >/dev/null 2>&1 ; pwd -P )"
fi

"${PROJECT_ROOT}"bin/docker/docker-compose down --remove-orphans
